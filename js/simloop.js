var output = document.getElementById("output");

function addToLog(logItem) {
    output.innerHTML += logItem + "<br>";
}

addToLog("Elevator Sim Starting");
addToLog("StartUp: Elevators=1, Floors=10");

